(jbuild_version 1)

(library
 ((name lib)
  ; this will be exported & installable as a library via OPAM
  (public_name ReasonNativeProject)
  (libraries (yojson core cohttp cohttp-lwt-unix lwt.unix))))
